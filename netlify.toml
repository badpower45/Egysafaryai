[build]
  base = "."
  command = "npm install && npm run build"
  publish = "dist/public"

[dev]
  command = "npm run dev"
  targetPort = 5173

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
