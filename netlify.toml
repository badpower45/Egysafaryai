[build]
  base = "."
  command = "cd client && npm install && npm run build"
  publish = "client/dist"

[dev]
  command = "npm run dev"
  targetPort = 5173

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
